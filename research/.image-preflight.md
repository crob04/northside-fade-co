# Image preflight — 2026-07-04T05:28:14Z

| Source     | HTTP | Hits on  |
| ---------- | ---- | ------------------------- |
| Pexels     | 200  | 8000                  |
| Pixabay    | 200 | 500                 |

**Verdict:** `both_ok`

**Banned sources** (do not use these as a fallback):
- `images.unsplash.com` direct URLs (no API key; bypasses `image-search` skill)
- `source.unsplash.com` (Source API unstable, 503)
- `cdn.pixabay.com` raw URLs (403 without API auth — must use `/api/` endpoint)

**Source policy** (per `image-search` skill):
1. Pixabay primary — `https://pixabay.com/api/?key=$KEY&q=...`
2. Pexels fallback — `https://api.pexels.com/v1/search?query=...` (raw key in `Authorization`)
3. Both APIs require `User-Agent: Mozilla/5.0` — Python `urllib` returns 403 from Pexels.
